{
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "server": "cd packages/server && npm run dev",
    "client": "cd packages/client && npm run dev"
  },
  "dependencies": {
    "superjson": "^2.2.1"
  }
}
